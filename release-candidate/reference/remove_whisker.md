# Remove whisker values

Remove whisker values

## Usage

``` r
remove_whisker(x)
```

## Arguments

- x:

  Named (`character`) input.

## Value

invisible `NULL`. Removes `x` from the whisker environment.
